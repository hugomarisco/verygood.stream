{
  "name": "root",
  "private": true,
  "scripts": {
    "lerna": "lerna",
    "bootstrap": "lerna bootstrap",
    "start": "lerna run start --stream",
    "build": "lerna run build",
    "build:watch": "lerna run build:watch --stream --parallel",
    "test": "lerna run test",
    "test:watch": "lerna run test:watch --stream --parallel",
    "test:ci": "lerna run test:ci && codecov",
    "clean": "lerna clean"
  },
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "@types/node": "^11.13.5",
    "@types/react-router-dom": "^4.3.2",
    "codecov": "^3.3.0",
    "jest": "^24.7.1",
    "lerna": "^3.13.3",
    "nyc": "^14.0.0",
    "prettier": "^1.17.0",
    "ts-jest": "^24.0.2",
    "ts-node": "^8.1.0",
    "tslint": "^5.16.0",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^3.4.4"
  },
  "dependencies": {
    "codem-isoboxer": "^0.3.7",
    "query-string": "^6.5.0",
    "winston": "^3.2.1"
  }
}
