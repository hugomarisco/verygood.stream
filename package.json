{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "test": "lerna run test --stream",
    "test:ci": "mkdir -p coverage && nyc --reporter=text-lcov lerna run test:ci > ./coverage/coverage.lcov && codecov",
    "lint": "lerna run lint",
    "clean": "lerna clean"
  },
  "devDependencies": {
    "@types/node": "^10.12.12",
    "codecov": "^3.1.0",
    "lerna": "^3.4.3",
    "mocha": "^5.2.0",
    "nyc": "^13.1.0",
    "prettier": "^1.15.3",
    "sinon": "^7.2.2",
    "tslint": "^5.11.0",
    "tslint-config-prettier": "^1.17.0",
    "typescript": "^3.2.2"
  },
  "dependencies": {}
}
