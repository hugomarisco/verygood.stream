{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "build-watch": "lerna run build-watch --stream",
    "test": "mkdir -p coverage && nyc --reporter=text-lcov lerna run test > ./coverage/coverage.lcov && codecov",
    "test-watch": "lerna run test-watch --stream",
    "clean": "lerna clean"
  },
  "devDependencies": {
    "@types/jest": "^23.3.11",
    "@types/node": "^10.12.12",
    "codecov": "^3.1.0",
    "jest": "^23.6.0",
    "lerna": "^3.4.3",
    "nyc": "^13.1.0",
    "prettier": "^1.15.3",
    "ts-jest": "^23.10.5",
    "tslint": "^5.11.0",
    "tslint-config-prettier": "^1.17.0",
    "typescript": "^3.2.2"
  },
  "dependencies": {}
}
